syntax = "proto3";

package strobesbufs.messier.analyzer;

import "strobesbufs/messier/common.proto";


message Wappalyzer {
    string target = 1;
    strobesbufs.messier.common.LogLevel log = 2;
    bool probe = 3;
}